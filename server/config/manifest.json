{
  "connections": [{
    "port": 8000,
    "labels": ["api"],
    "routes": {
      "cors": true
    }
  }],
  "plugins": [
    {"blipp": null },
    {"inert": null},
    {"./auth": null },
    {"./groups": [{
      "routes": {
        "prefix": "/api/v1/groups"
      }
    }]},
    {"./requests": [{
      "routes": {
        "prefix": "/api/v1/requests"
      }
    }]},
    {"./search": [{
      "routes": {
        "prefix": "/api/v1/search"
      }
    }]},
    {"./users": [{
      "routes": {
        "prefix": "/api/v1/users"
      }
    }]},
    {"./services": [{
      "routes": {
        "prefix": "/api/v1/services"
      }
    }]}
  ]
}